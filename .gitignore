# Real personal financial reference data — never commit actual values.
# Only *.example.json templates (fake data) are tracked.
data/local/*.json
!data/local/*.example.json

# Secrets
.env
.env.*
!.env.example

# Python (backend/)
backend/.venv/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
